<h1>Ordr creation</h1>

<form action="{{ path('admin_ordr_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_ordr') }}">
            Back to the list
        </a>
    </li>
</ul>
